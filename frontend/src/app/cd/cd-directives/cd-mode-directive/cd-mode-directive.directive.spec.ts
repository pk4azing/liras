import { CdModeDirectiveDirective } from './cd-mode-directive.directive';

describe('CdModeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CdModeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
