import { CcdModeDirectiveDirective } from './ccd-mode-directive.directive';

describe('CcdModeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CcdModeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
